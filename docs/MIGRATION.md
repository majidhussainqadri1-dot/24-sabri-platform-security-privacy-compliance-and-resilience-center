# Migration — Foundation 0.26.0 to 0.27.0

1. Back up database and plugin files; record the exact 0.26.0 package/hash.
2. Deploy only to staging.
3. Upgrade to 0.27.0 and run activation/schema/system checks.
4. Verify existing security states, audit gaps, governance decisions, controls, assurance records and privacy requests remain intact.
5. Exercise new callback authority, deletion retry step-up, concurrency and evidence-gap paths.
6. Run full automated, browser, accessibility, restore and rollback acceptance.
7. Roll back code only with compatible evidence interpretation; never clear unresolved gaps to force success.
8. Production promotion requires Founder approval after staging evidence.
