<?php
declare(strict_types=1);
require_once __DIR__ . '/bootstrap.php';require_once dirname(__DIR__) . '/plugin/sabri-security-center/src/Support/Sanitizer.php';require_once dirname(__DIR__) . '/plugin/sabri-security-center/src/Storage/AuditLogger.php';require_once dirname(__DIR__) . '/plugin/sabri-security-center/src/Storage/AuditGapStore.php';
use Sabri\Platform\Security\Storage\AuditGapStore;use Sabri\Platform\Security\Storage\AuditLogger;
$n=0;function c45(bool $v,string $m):void{global $n;++$n;if(!$v){fwrite(STDERR,"FAIL: $m\n");exit(1);}}
c45(!AuditGapStore::record('other_option','risk','one','invalid'),'Non-audit-gap options must be rejected.');c45(AuditGapStore::record('spcrc_cycle45_audit_gap','risk','case-1','rollback_failed',['token'=>'secret','email'=>'a@example.com','safe'=>'bounded']),'Valid bounded custom operational gap must persist.');$all=AuditGapStore::all('spcrc_cycle45_audit_gap');c45(count($all)===1,'Custom operational gap must remain independently countable.');$gap=current($all);c45(($gap['context']['token']??'')==='[REDACTED]'&&!isset($gap['context']['email']),'Sensitive gap context must be redacted.');c45(($gap['context']['safe']??'')==='bounded','Safe bounded gap context must remain.');$id=(string)array_key_first($all);c45((bool)preg_match('/^[0-9a-f-]{36}$/',$id),'New gap identity must be UUIDv4.');$bad=AuditGapStore::reconcile('spcrc_cycle45_audit_gap',$id,'vault:evidence','file00:stepup',new AuditLogger());c45(is_wp_error($bad)&&$bad->get_error_code()==='spcrc_audit_gap_invalid','Only explicit managed categories may use generic reconciliation.');c45(AuditGapStore::count('spcrc_cycle45_audit_gap')===1,'Rejected reconciliation must leave evidence intact.');
echo "PASS: $n Cycle 45 audit-gap boundary assertions\n";
