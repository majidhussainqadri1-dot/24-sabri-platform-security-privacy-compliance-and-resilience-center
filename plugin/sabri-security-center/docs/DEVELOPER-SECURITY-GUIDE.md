# Developer Security Guide

Use canonical native-owner commands, server-side capabilities/object checks, prepared queries, bounded validation/encoding, nonce plus authorization, same-origin/SSRF controls, idempotency/replay protection, redacted events, opaque evidence references and fail-closed degraded modes. Never treat a manifest, UI state or cache as authorization.
